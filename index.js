/**
 * @format
 */

import {AppRegistry} from 'react-native';
import AppContainerMenu from './src/frontend/routes/routes';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => AppContainerMenu);
